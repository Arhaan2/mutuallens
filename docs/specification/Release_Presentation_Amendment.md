# Release presentation and execution amendment

The September 18, 2026 owner request supersedes requirements for customer-facing demo, preview, prototype, internal verification, release-gate, build-stamp or development-status chrome. Keep the existing visual identity and useful upload product. Remove public synthetic-result entry points; retain synthetic fixtures and accurate sample provenance on imported datasets.

Capability, presentation and release state are separate. Automatic collection is available only when real source, account, completeness, scale, capacity and hosted evidence satisfy the applicable gates. Otherwise show a concise feature-level unavailability message with a working upload alternative. Keep known input limitations and privacy disclosures accurate. Presentation cleanup must not enable provider starts, indexing, ads or trackers.

Uploads compare immediately without required account identity, dates or completeness acknowledgment. Preserve format compatibility, missing-direction versus empty-list behavior, provisional partial inputs, exports, all-record pagination, optional snapshots and the previous report after replacement failure. Imports remain in browser memory unless explicitly saved locally.

Use coordinated isolated implementation and independent review. Preserve current main with a unique annotated remote tag before edits; preserve owner work and existing deployments. Verify the exact PR head, merge normally without overrides, verify resulting main CI, deploy the existing authorized resources and test the stable hosted URLs. No spending, new accounts, upgrades, overage, paid fallback, new legal acceptance or unrelated infrastructure changes.

The Cloudflare public and checker origins remain separate. GitHub Pages is a static project entry with a normal link to the Cloudflare product, never a proxy, redirect, application host or duplicate commercial surface. All unfinished launch pages remain noindexed; checker, results, APIs and staging always remain noindexed. Ads and trackers remain off.

The single authoritative execution record is [release-current.md](../release-current.md). Prior evidence remains historical. A shipped presentation cleanup with blocked automatic acceptance must never be described as a complete automatic-product release.
